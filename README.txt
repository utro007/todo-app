Pogled v bazo preko dockerja.
mysql -u root -ppassword
SHOW DATABASES;
USE todo_db;
SHOW TABLES;
SELECT * FROM todos;

NAL2 paginacija prikazov. NPR najprej se naložita 5 nalog in uporabnik klikne na "Naprej" in naloži več nalog